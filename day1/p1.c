#include<stdio.h>

int main() {

    int num1 = 0;
    long 
    scanf("%d", &inutmum);
    if(inutmum % 2 == 0)
        printf("%d is Even", inutmum);
    else
        printf("%d is not Even", inutmum);
    return 0;
    
}